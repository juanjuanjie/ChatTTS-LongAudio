# 复制本文件为 .env 后启动 app.exe
# ChatTTS 服务地址
WEB_ADDRESS=localhost:9966

# 代理绕过本地服务
NO_PROXY=localhost,127.0.0.1,::1
no_proxy=localhost,127.0.0.1,::1

# 是否使用编译模式（一般保持 false）
compile=false

# 默认音色：可以是数字 seed，也可以是 speaker/ 目录下的音色文件名
# 本地使用时可在 .env 中覆盖，例如 DEFAULT_VOICE=3798
DEFAULT_VOICE=2222
